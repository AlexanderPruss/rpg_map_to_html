# Different background color

Unlike the basic example, this one uses black as the background color. 
This is done by passing in `map_image.skip_empty_cells.empty_color_rgba` to the config - see `last_used_config.json`.

# Running the example

Run the binary from this directory. It will prompt you if you want to use the last used config; say yes.